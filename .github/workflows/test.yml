name: Test
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  units:
    name: Units
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - run: npm ci
    - run: npm test
