name: 'Recognition'
description: 'Allocate labels depends on changed files'
inputs:
  entity:
    description: 'Entity to output in matrix'
    required: false
    default: 'project'
  token:
    description: 'GitHub token'
    required: true
  source:
    description: 'Label metadata filename'
    required: true
outputs:
  matrix:
    description: 'Matrix in JSON format'
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  icon: target
  color: gray-dark
